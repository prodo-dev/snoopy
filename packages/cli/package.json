{
  "name": "@prodo/snoopy-cli",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc --build",
    "clean": "rm -rf lib",
    "lint": "set -ex; tsc --build; tslint --project ."
  },
  "dependencies": {
    "@prodo/snoopy-server": "1.0.0",
    "yargs": "^13.2.2"
  },
  "devDependencies": {
    "@types/yargs": "^13.0.0",
    "ts-node": "^8.1.0"
  }
}
