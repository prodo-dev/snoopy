{
  "name": "@prodo/snoopy-search",
  "version": "1.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc --build",
    "clean": "rm -rf .cache lib",
    "test": "jest",
    "lint": "set -ex; tsc --build; tslint --project ."
  },
  "dependencies": {
    "glob": "^7.1.3",
    "minimatch": "^3.0.4"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/jest": "^24.0.11",
    "@types/minimatch": "^3.0.3",
    "jest": "^24.7.1",
    "jest-dom": "^3.1.3",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "jsdom"
  }
}
