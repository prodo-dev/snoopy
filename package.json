{
  "name": "@prodo-ai/snoopy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn workspace @prodo-ai/snoopy-cli start",
    "build": "tsc --build",
    "build:watch": "tsc --build --watch",
    "test": "wsrun --exclude-missing --serial test",
    "lint": "wsrun --exclude-missing --serial lint",
    "postinstall": "patch-package"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "patch-package": "^6.1.2",
    "postinstall-postinstall": "^2.0.0",
    "prettier": "^1.17.0",
    "tslint": "^5.16.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.4.3",
    "wsrun": "^3.6.5"
  }
}
